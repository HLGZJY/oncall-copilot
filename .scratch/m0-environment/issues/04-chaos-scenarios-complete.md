Status: ready-for-agent
Blocked by: 03

# 04 注入框架泛化 + 剩余剧本补齐（至 8+）

## 任务

从 01/02 剧本提炼通用模式后，补齐剩余剧本至 ≥8 类，覆盖 m0-execution 六大类：

| # | 剧本 | 注入手段 | 类别 |
|---|---|---|---|
| 03 | OOM | Pumba | 资源类 |
| 04 | 下游超时 | Pumba | 网络类 |
| 05 | 网络丢包 | Pumba | 网络类 |
| 06 | 死锁 | 自定义脚本 | 业务类 |
| 07 | 队列堆积 | Locust/k6（容器化） | 负载类 |
| 08 | 连接打满 | Locust/k6 | 负载类 |
| 09 | 进程被杀 | Pumba | 故障类 |
| 10 | 缓存雪崩 | 自定义脚本 | 故障类 |
| 11 | **版本协议不兼容（语义层，必做）** | 自定义 | 业务语义层 |

## 要点

- 语义层剧本的特性：指标日志都正常但业务出错——告警规则需覆盖日志关键字或业务指标维度
- 每个剧本必须在 `scenario.yaml.expected_alerts` 写清预期告警

## 验收（机械判定）

- [ ] `chaos/scenarios/` 下 ≥8 个目录（不含黄金集），六类全覆盖，语义层必须有
- [ ] 每剧本注入 → 预期告警触发 → cleanup 恢复，可重复执行；`scenario.yaml` 全过校验器
- [ ] 无哑剧本：每个剧本至少对应 1 条已配置的告警规则

## Blocked by

03
