# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# ---- 虚拟环境 ----
.venv/
venv/
env/

# ---- 环境变量与密钥 ----
.env
.env.*
!.env.example

# ---- Docker (M0 起) ----
docker-compose.override.yml
.dockerignore

# ---- 数据与临时区 ----
# 注意：.scratch/ **不忽略** —— 它是本仓库的 issue 追踪器，必须纳入版本控制才可追溯。
# 约定见 docs/agents/issue-tracker.md
# 告警 webhook 落盘是运行时产物（alert-dumper 容器追加写），不入库
deploy/alerts-dump.jsonl
data/
*.log
# SQLite dev 库是运行时产物（ONCALL_DATABASE_URL 默认 sqlite:///./oncall.db），不入库
*.db
# M2 issue 07：重建库（补 classification_json 列）前的旧库备份，同样是运行时产物
oncall.db.bak*

# ---- 操作系统 (Windows) ----
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ---- 编辑器 / IDE ----
.vscode/
.idea/
*.swp

# 本地工作明细（派工 prompt / e2e 实测 json），按 M5 先例不进版本库
.scratch/tmp/

# 仓库损毁取证遗留（2026-09-09，勿提交勿删除）
.git-broken-20260909/
