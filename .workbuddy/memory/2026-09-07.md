# 2026-09-07

## M1 issue 03：主指纹 + 时间窗去重合并（00:40–01:30）

- TDD 红绿：新增 `tests/unit/test_ingest_fingerprint.py`（指纹纯函数：稳定性/区分度/0xFF 防碰撞/时间窗桶）与 `tests/unit/test_ingest_dedup.py`（3 连发合并/超窗新行/窗口可配/resolved 联动/幂等回归 + 端点层窗口配置），红灯后实现 `src/oncall/ingest/fingerprint.py`，并改造 normalize/service/app（移除 02 的原始标识临时键 `raw_identity_fingerprint`）。
- 主指纹（G1）= sha256(canonical label 子集 {alertname, instance, job} 存在者参与 + 字典序 + 0xFF 分隔 + **时间窗桶**)。含桶的原因：fingerprint 带唯一约束，只按 label 算则「超窗新行」与 dedup_window 无法表达 → 已登记 **D-14**。
- 合并（G2）取「当前桶 + 前一桶」两个候选指纹 → 滑窗语义（按距上次 firing 计，非距首 firing）。
- 计数口径 → 登记 **D-15**：仅 fired_at 晚于 last_fired_at 才 dedup_count++；原样重放/旧投递跳过不计数（D-13 幂等）；resolved 不计数，但 startsAt 晚于已见 firing 时补计（漏收兜底）。
- 实测：真实 alert 连发 3 次 → 1 行 dedup_count=3、last_fired_at 刷新、resolved 落库；+11min → 新行；dump 232 条真实 payload 回放 received 232 / deduped 167 / 落库 65 行（8 种 labels，单最大 dedup_count=6）。
- **0 漏收边界取证**（调试铁律：不猜）：库内计数 120 vs dump 内 firing 告警 116 → 脚本证实多出的 4 条是「4 条 resolved 通知无对应 firing」（firing 通知未被采集到）的补计，重复 firing 投递实测 0 条。
- 门禁：104 passed / coverage 98%（ingest 模块 94–100%）/ ruff check+format 全绿；提交 ac65aec；issue 03 置 resolved（验收逐条实测回填 + 设计取舍与实测补充两节）。
- 划窗语义踩坑自记：滑窗是「距上次 firing ≤ 窗宽」，写边界用例时按上次 firing 时间算，不是按首 firing。
- 联调口径沿用 02：`PYTHONPATH=src ONCALL_DATABASE_URL="sqlite:///./ingest-itest.db" python -m uvicorn oncall.ingest.app:create_app --factory --port 8010`（curl/urllib 需 no_proxy，dump 有 7 行截断碎片需跳过）。

## M1 issue 04：上下文三源 + HTTP 收口（01:35–02:35）

- TDD 红绿：19 个单测（FakeFetcher 注入，**A1 守卫禁止单测 import httpx**，故协议注入而非 MockTransport）→ 落 `src/oncall/context/`（promql/metrics/topology/changes/service/config/models）与 `src/oncall/infra/http.py`。
- **C4 守卫是本票的隐形前置**：httpx 全仓只许在 `oncall/infra/http.py`——该收口文件此前不存在，本票一并落地（Fetcher 协议 + HttpxFetcher，trust_env=False 防代理拦 127.0.0.1）。
- **诊断战果**：C4 守卫白名单路径 bug——白名单写 `oncall/infra/http.py` 但 `_rel_module` 返回 `src/` 前缀路径，白名单从未生效；守卫报错对象恰为白名单目标文件，证实后改为 `src/oncall/infra/http.py`。另补回 decisions.md 缺失的 D-13 行（架构文档引用悬空）。
- 统一形状 `{source, status: ok|unavailable, items, meta}`；降级纪律实测：stop prometheus 后 metrics/topology=unavailable+reason、changes 仍 ok、exit=0 不抛错。窗口 30m/URL/超时全走 ContextConfig env → 登记 **D-16**。
- 联调：回放 dump 末 8 条有效 webhook 落库（received 8 / deduped 6，03 语义未破坏）→ 真实告警 #2 跑三源（labels 无 instance/job 时 selector 回落 alertname 路径实测验证）；integration 4/4 过（`ONCALL_RUN_INTEGRATION=1`，默认 skip）。
- 门禁：123 passed / coverage 96.25% / ruff 全绿；提交 **e7df9eb**；issue 04 置 resolved（验收逐条实测回填）。
- 踩坑新增：Git Bash `/tmp` 与 Windows python 路径不一致（`/tmp/x.py` 被 python 解析为 `F:\tmp`）——临时脚本放 `.scratch/tmp/` 用完即删。
- 交接：下一票 issue 05（alert card 契约）直接消费 `collect_context(labels, config=ContextConfig.from_env())` 的 `{"sources": [...]}` 形状 + `ingest/app.py` 的 env 配置模式；`GET /alerts/{id}/context` 组装时 PromClient/HttpxFetcher 可直接复用。

## M1 issue 05：事件卡片 JSON 契约 + 查询路由（02:40–03:20）

- TDD 红绿：11 个路由单测（`tests/unit/test_alert_card_api.py`，inproc_asgi 标记，FakePromClient 注入——不 import httpx）→ 落 `src/oncall/api/`（card.py 组装 111 行 + routes.py 薄路由 46 行），`ingest/app.py` 挂载并新增 `context_config`/`context_client` 工厂注入口。
- 契约键名登记 **D-17**：顶层 `{alert, context, generated_at}`；alert 本体 13 键（annotations/am_fingerprint/raw_alert/webhook 为 annotations_json 溯源展开）；**上下文时间锚 = last_fired_at**（卡片描述「告警发生时」的状态，回放历史 dump 有意义——这是本票唯一需要拍板的语义点）。
- `GET /alerts` 列表一并落地：`{items, total, limit, offset}` + fingerprint 过滤，limit 钳 [1,100]；列表只给本体不给上下文（防逐行打 Prometheus）。
- **悬空引用修复**：issue 04 提交信息与备注声称「登记 D-16」但 decisions.md 实际漏写该行（grep 全仓只有 config.py docstring 引用）——本次补记 D-16（三源统一形状 + 配置口径），状态列注明补记缘由。
- 联调实录：uvicorn(8010) + curl 真实卡片（oncall.db 告警 #1/#2 均 dedup_count=2）；`docker compose stop prometheus` → metrics/topology unavailable + reason、changes 恒 ok（占位不触网）、接口 200；恢复容器回 ok；404 实测。
- 测试替身踩坑：FakePromClient 属性 `targets`（list）遮蔽同名方法 → TypeError，且 TypeError 不在 UNAVAILABLE_ERRORS 降级范围（这是 issue 04 的预期设计，只有已知失败降级）——替身属性改 `_targets`。
- 门禁：全量 pytest 绿 / coverage 96.70% / ruff check+format 全绿 / C6 行数达标。
- 交接：下一票 **issue 06 receiver 切换**（依赖 M0-05 黄金集——开工前先核对其状态）：双写开关在 deploy/ 侧，卡片/ingest 侧 API 已就绪无需再动；切换时点避开黄金集采集（G6）。

## M0-05 黄金集双盲复核（08:10–08:30）

- 应用户要求对 11 剧本黄金集标注做双盲复核：先只读 inject.sh/cleanup.sh 独立推导根因，再对比标注；4 项脚本核对（golden vs scenario.yaml 逐字 / timeline vs expected_alerts 集合差 / _raw+全量 dump 缺口终审 / holdout 一致性），脚本放 .scratch/tmp/ 用完即删。
- **逐字钉死**：11 剧本 root_cause/remediation 与 scenario.yaml expected_* 逐字一致（上一轮口头说 cache-avalanche 含实测扩展是 YAML 行折叠的目测假象，已更正）——「预期=结果」是同义反复，核对价值在独立证据。
- **P1 发现**：3 剧本预标注声称的级联告警实测 0 firing（queue-backlog 的 StuckPending / downstream-timeout 的 QueueDepthHigh+StuckPending / packet-loss 的 QueueDepthHigh）——DB 变慢同时拖慢提交与消费，净堆积不越 for:2m 阈值，级联不成立；需修 scenario.yaml 口径。
- **P2 发现**：cache-avalanche 与 pool-exhaustion 的 investigation_path 疑似跨剧本复用且与机理不匹配；timeline 的 scenario 标签是 rules.yml 静态标注，M7 归属判定禁用。
- 2 处 resolved_at=null 经终审为 dump 确无 resolved 通知，如实记录；holdout 11 文件标注与 dev 一致、r3 独立。
- 核对报告已写入 M0-05 Comments，issue 保持 ready-for-human（修正裁决 + 双签权在人）。

## 黄金集标注校准设计（08:25）

- 用户裁决暂缓 issue 06，先针对双盲复核 P1/P2 发现出设计方案 → 按 feature-design-template 落 `docs/design/golden-set-calibration-design.md`（status=draft，待评审）。
- 关键事实核实：D-12 契约是 `ScenarioSpec` 九字段 + `extra="forbid"`；`investigation_path` 只在 golden 侧手写、scenario.yaml 无权威源——这是 P2 跨剧本复用的结构性根因；校验器缺「timeline alertname ⊆ expected_alerts」规则——P1 从这条缝漏进来。
- 方案要点：P1 采方案 A（expected_alerts/expected_root_cause 收敛到 dump 实测，零重采；B 增强注入重采列为 M2+ 备选，C 双轨标注不推荐）；P2 采「权威源前移」——scenario.yaml 新增 `expected_investigation_path`（九字段→十字段，需登记 D-18），golden 逐字复制 + `golden_matches_scenario` 升级逐字段校验 + `load_golden_tree` 增 R6。path 定制判据：第 1 步必须指向 inject.sh 直接产生的首个可独立观测信号（11 剧本切入点表已列）。
- 设计前置依赖链：本设计评审通过 → 修正实施 → M0-05 人工抽核双签置 resolved → issue 06 解除门槛。

## 黄金集校准实施（08:40–08:55，方案 A 裁决后当日落地）

- TDD 红绿：D-18 契约测试先行（TestExpectedInvestigationPath/TestAnnotationConsistency/TestR6CrossValidation + catalog 真实数据守卫）→ schema.py 落 `expected_investigation_path` 字段、`golden_matches_scenario` 逐字段比对、`load_golden_tree(scenarios_dir=)` R6（不传时仅树校验向后兼容），拆 `_load_specs/_check_pair_rules/_cross_check_slug` 过 PLR0912。
- 数据校准脚本（.scratch/tmp/ 用完即删）：11 scenario.yaml 插入定制 path（第 1 步=inject.sh 首个可观测信号）+ P1 三剧本 expected_alerts/root_cause 收敛到 dump 实测；22 golden 三字段同步。
- **新踩坑（重要）**：`yaml.safe_dump` 折行中文长文本会追加 `...` 文档结束标记（YAML 非法）且折行处读回会引入空格改变文本——长中文文本 dump 必须用 `width=10**9` 单行输出；另 `yaml.safe_load` 读回的文本与折行原文不等值，逐字校验必须基于 load 后的值而非源文本比对。
- Git Bash heredoc 长脚本会报 unexpected EOF 但实际可能已执行——执行后必须先 `git status` 核实实际改动再决定下一步，不能按报错判断未执行。
- 门禁：143 passed / 4 skipped，coverage 95.89%，ruff 全绿；schema.py 275 行（C6 达标）。
- D-18 已登记 decisions.md；校准设计 status→implemented 验收回填；M0-05 Comments 追加落地记录（仍 ready-for-human 待人工双签）。

## M1 收尾：issue 06 receiver 切换 + 端到端演练（09:27–10:35）

- 用户拍板两个门槛：M0-05 并行放行（采集已完毕，抽核/双签由人并行推进）；部署形态选 oncall 收编进 compose（volume 挂 src + 仓库根挂 /app/repo 保 oncall.db 宿主可见，python:3.11-slim 对齐 C1，清华源装依赖）。
- **receiver 三档 env 开关（纯 yaml，无 Python 开关逻辑→演练实录作验收证据）**：alertmanager.yml（dump 默认/回退）/ -dual.yml（双写）/ -oncall.yml（终态）；compose 挂载 `${ONCALL_AM_CONFIG:-alertmanager.yml}` 插值，切换=带 env up -d alertmanager，默认 dump 安全默认。
- 演练实录（02-slow-sql ×5 循环，DURATION=240，告警归零才进下一步）：dual 档 3 循环 → id 3/4/5 dedup_count=3（DemoTasksHighLatency firing 10:02:27/10:07:21/10:11:13）+ DemoHighErrorRate 迟触发计入 0 漏收；dump 回退档 DB 纹丝不动、collect_run.sh 依赖面完好；oncall 终态档 DB 新增 id 7–10、dump 无串档。GET /alerts/5/context 三源齐全（metrics 3/topology 2/changes 空）。
- **诊断记录（被证实的假设）**：oncall 档 firing 未并入旧行 ≠ bug——D-14 指纹含时间窗桶（创建桶固定，候选=当前+前桶），跨两桶 firing 开新行（"保守不漏收"）；dump +5 行全是切档前迟到通知（endsAt ≤02:19:06Z < 切档 02:19:15Z）。演练时间点若跨两个 10m 桶边界，3 连发可能开 2 行——重做同类验收时用循环间隔 <10min 且留意桶边界（:00/:10/:20）。
- 收尾：设计文档验收节 8 条全回填 + status→implemented；spec.md Phase C 勾；issue 06 Status→resolved。门禁 pytest 147（143 passed+4 skipped）coverage 95.89% ruff 全绿。
- **M1→M2 交接**：M2 消费 alert_events（status: deduped → classified），分母=dedup_count、0 漏收已保证；PromClient/三源客户端在 src/oncall/context/ 可复用；容器终态 9 容器含 oncall（127.0.0.1:8010）。

## M2 设计阶段（本次会话，2026-09-07）

- **交付**：`docs/design/m2-denoise-classify-design.md`（status: reviewed，G1–G9 定案 + R1–R8 评审依据 + T1–T7 拆解）；`.scratch/m2-noise-reduction/`（spec + issues 01–07）；decisions.md 登记 D-19/D-20/D-21；CONTEXT.md 新增 4 术语（逻辑告警/规则通道/LLM 通道/误报剧本）。
- **关键定案**：M2 只做分类层（不做抑制/聚合独立层，G1）；规则只判误报/放行、不判真实（G2）；risk 由置信度阈值（默认 0.7）派生、LLM 只输出 false_positive|incident（G3/D-19）；`alert_events` 增列 classification_json、status 枚举不扩（G4/D-19）；incidents 1:1 最小集（G5）；统计层「逻辑告警」传递归并对冲 D-14 桶边界跨桶分行，降噪率=(R−I)/R、R=Σdedup_count（G6/D-20）；dev 集新增误报剧本 false-positive-flap（规则阈值漂移型）+ golden timeline 可选字段 classification（G7/D-21）；单次 LLM 成本上限 ¥0.05 估算法（G8）。
- **就绪态**：01–05/07 = ready-for-agent；06 = ready-for-human（D-12/D-18 契约面扩展）。
- **会话口径（用户拍板）**：设计期零真实 LLM 调用，全 mock/估算；issue 07 真实调用前需用户确认 key。
- **提交**：d4392af 设计文档 / 0e42e50 决策+术语 / e369a71 tracker；门禁 pytest 147（4 skipped integration）+ ruff 全绿，工作区干净。
- **M3 交接要点**：incidents 最小集 `id/alert_ids/severity/status/created_at`（1:1 建档，alert_ids[0] 锚 D-17 卡片）；调查入口按 incident_id 启动；`GET /incidents` 是查询面。

## M2-A issue 01：分类领域模型与 LLM 接缝（T1 完成）

- **提交**：57564bf `feat(M2-降噪分类): 分类领域模型与 LLM 接缝（issue 01）`（基线 c5f7e9e），issue 01 Status → resolved，工作区干净。
- **落位**：`src/oncall/classify/`——models.py（Verdict 三态 / ClassificationChannel / LLMVerdict 契约 / LLMCallMeta / ClassificationResult，StrEnum）、deriver.py（derive_verdict 纯函数，严格小于阈值才派生 risk，classified_at 可注入）、client.py（LLMClassifier Protocol + MockLLMClassifier：script 按序回放 LLMVerdict/异常混排，耗尽回落默认；LLMOutputError/LLMTimeoutError 夹具）、__init__.py 公开面。
- **测试**：tests/unit/test_classify_{models,deriver,mock_client}.py 共 22 例；全量 pytest 96.31%（classify 4 文件 100%）+ ruff check/format 绿。
- **实现注记**：枚举成员大写（Verdict.RISK）值为小写串；G3/D-19 词汇未新增 CONTEXT 术语；零真实 HTTP、零 schema 变更。
- **解锁**：issue 02（规则通道）/ 03（LLM 通道）解除阻塞，二者只依赖本票公开面（derive_verdict + Mock 夹具），可并行。

## M2 issue 02：规则通道谓词注册表（T2，G2 定案）

- **提交**：213a38f `feat(M2-降噪分类): 规则通道谓词注册表（issue 02）`（基线 eea0116），issue 02 Status → resolved，工作区干净。
- **落位**：`src/oncall/classify/rules/`——registry.py 单文件（RuleVerdict frozen dataclass 二值结构 / Rule{name, predicate(alert_event, context)} / RuleContext{now, maintenance_windows} / MaintenanceWindow{starts_at, ends_at, labels} / DEFAULT_RULES 3 条 / run_rule_channel 编排）；classify/__init__.py 仅追加导出。
- **关键判定信号**：幽灵通知 = annotations_json["raw_alert"]["status"] == "resolved"（M1 合并逻辑把无锚点 resolved 兜底计为新 firing，M1 issue 03 实录 4 条）；维护窗口 = labels 选择子 + last_fired_at 闭区间；失效 = resolved_at 严格早于 context.now（恰等于放行，测试钉死）。
- **取值约定**：命中行 ClassificationResult = confidence 1.0 / tokens 0 / cost_cny 0 / model "rule"（规则名经 reason 前缀 [name] 归因，model 维度聚合不碎化）/ classified_at=context.now。
- **测试**：tests/unit/test_classify_rules.py 16 例；全量 pytest 112 passed，coverage 96.55%（registry.py 99%）+ ruff 双检绿。
- **术语**：CONTEXT.md 新增「规则名 / Rule Name」词条（resolved_only_ghost / maintenance_window / stale_replay）。
- **解锁**：issue 04（落库/incidents/POST /classify）的规则通道侧阻塞解除；03（LLM 通道）与本票无依赖可并行。

## M2 issue 03（T3 · LLM 通道 mock 编排）——resolved @ a1bb7d6

- **落点**：`src/oncall/classify/llm/`（prompt.py / fewshot.py / channel.py）+ 三组单测（20 例）+ classify 包导出仅追加。
- **编排契约**：畸形输出（LLMOutputError）重试 ≤2 次（总尝试 3）→ risk/llm_error；超时（LLMTimeoutError，30s）**不重试**直接落 risk（3×30s 击穿延迟/成本预算）；低置信走 01 的 derive_verdict 派生 risk（channel=llm）。兜底值钉死：confidence=0.0、reason 两常量、tokens=估算输入×尝试次数、cost 按单价快照。
- **泄漏守卫四重闸**：3 验证剧本排除（含 06 落地后回归断言）/ loader 目录名硬守卫（非 dev 即 ValueError）/ 禁复用会读 holdout 的 load_golden_tree（源码级断言）/ 真实 dev 集实测排除。
- **成本 G8 估算法**：estimate_tokens 2 字符≈1 token；MOCK_MODEL_PRICING_CNY_PER_1K = deepseek-chat (0.002, 0.003) / qwen-plus (0.0008, 0.002) CNY/千tokens（取价 2026-09-07）；未登记单价构造期 ValueError，禁静默 0 成本。
- **每态 K=2**（G3 允许 2–3 取下限控 4k token 预算）；prompt 五段结构，同输入逐字确定（快照测试）。
- **术语**：CONTEXT.md 新增「few-shot 样本池 / Few-Shot Pool」「验证剧本 / Validation Scenario」。
- **测试**：201 passed / 4 skipped，coverage 96.69%，ruff 双检绿。
- **解锁**：M2-A（T1–T3）全部完成；issue 04（落库/incidents/POST /classify）解除全部阻塞；05（统计）依赖 04。
- **坑**：同一文件并行 Edit 两次丢改动（channel.py 导入、llm/__init__ 导入块、issue 验收回填共 3 次 EBUSY/丢失），均靠 grep 复核后串行重补——多编辑必须串行。

## M2-B issue 04（T4 · 落库与 incidents 建档 + 分类 API）——resolved @ 1d27271

- **落点**：`src/oncall/classify/service.py`（双通道编排，新增）+ `src/oncall/db/`（models 增 classification_json 列与 Incident 表，views.py 新增）+ `src/oncall/api/`（routes 三件 + card 查询扩展）+ `ingest/app.py`（classify_runtime 注入口）。
- **编排契约（G4）**：规则通道先行（命中直判，0 次 LLM）→ 未决行进 LLMChannel → classification_json + status 同事务逐行提交；incident 判定同事务 1:1 建档（G5 最小五字段，severity 缺省 warning）；已 classified 行跳过（幂等，llm_calls=0）；请求外 id 保持 NULL+deduped。
- **API 契约**：POST /classify `{alert_id}` 或 `{batch: "all"|"pending"}`（二选一 422、未知 id 404、**runtime 未注入 503 禁静默 mock**）→ 恰好五键 `{classified, false_positive, risk, incident, llm_calls}`（classified 只计新分类）；GET /alerts?verdict= 走 json_extract（R7 不预优化，NULL 行不被任何 verdict 命中）；GET /incidents 为 M3 调查入口查询面。
- **结构解法**：alert_body/incident_body 下沉 `db/views.py` 解 classify→api 循环导入（card.py 再导出保 M1 公开面）；LLM 通道+编排环境收敛 `ClassifyRuntime` 单参数（max-args 先例）；`POST /classify` 失败行兜底语义完全在通道层，服务层不改写。
- **加分项**：架构守卫新增 ingest 主链路（normalize/fingerprint/service/schemas）零 oncall.classify 零 LLM SDK AST 断言（app.py 组装点豁免）；DB 契约测试扩 D-19（11 列 + incidents 表）。
- **回写**：架构 §4（M2 增列 + incidents 已建表标注）；CONTEXT.md 新增「双通道编排 / Dual-Channel Orchestration」。
- **测试**：新增 31 例（service 15 + api 16）；全量 **228 passed / 4 skipped，coverage 97.11%**，ruff check+format 双绿。门禁发现既有 DB 契约测试钉死 D-13 列集，随 D-19 同步扩展。
- **坑①持续实锤**：本会话同文件并行/连续 Edit 多次报成功未落盘（test_architecture_guards 常量块、classify/__init__ 导入块、test_classify_api _make_client、routes create_router 签名），全部靠 grep 复核后串行重补。**同文件编辑必须一回合一个 Edit，下一回合 grep 复核。**
- **解锁**：M2-B 仅剩 05（统计口径，依赖本票落库形态 classification_json/incidents）；07（端到端验证）需用户先确认真实 LLM API key。

## M2-B issue 05（T5 · 统计口径：逻辑告警归并 + 降噪率/漏报核算）——resolved @ 7edc8b7

- **落点**：`src/oncall/classify/stats.py`（纯函数统计层，新增）+ `classify/__init__.py` 仅追加导出 + `tests/unit/test_classify_stats.py`（22 例）+ CONTEXT.md §四 + issue 05 票面回填。零 schema 变更、零 LLM 调用、M1 落库语义零改动。
- **归并契约（D-20）**：canonical 子集 `{alertname, job, instance}`（同 D-14 指纹输入不含桶，falsy 跳出）分组 → 组内按 fired_at 排序 → 相邻行 `后.fired_at − 前.last_fired_at ≤ dedup_window` 传递连链（恰等窗口归并，> 断链）；只发生在统计层。
- **口径**：R = Σ dedup_count（不是行数，否则 M1 行内合并贡献漏出分子、80% 失真）；I = incident 逻辑告警数；降噪率 = (R−I)/R，R=0 返回 None + `denoise_rate_defined=False`。漏报 = golden incident 被判 fp；risk 单列 risk_observed；另加 false_alarms（golden fp 被判 incident）与 unmatched（无 golden 覆盖，不臆断漏报，issue 07 据此拦截数据缺口）。
- **关键设计**：`LogicalAlert.effective_verdict` 收敛判定 incident > risk > unclassified > false_positive（incident 优先 = 0 漏报口径；risk 优先于 fp = D-20 不算漏报的对偶）；`GoldenAnnotation` 与分类结果显式分形（D-18），缺省 classification=incident（D-21）；`snapshot_alert_rows` session 块内物化 `AlertStatsRow` 普通值防 detached（坑⑥）；时间统一走 `as_utc`（坑⑧，naive 按 UTC）。
- **测试**：新增 22 例（归并 8 + 收敛判定 4 + 指标 3 + golden 6 + 快照 1）；全量 **250 passed / 4 skipped，coverage 97.34%**，ruff check+format 双绿。
- **新坑⑨**：naive `datetime(...)` 构造触发 DTZ001——测试里用 `at(h,m).replace(tzinfo=None)` 代替 noqa 更干净；ruff I001 会被 `--fix` 自动重排 import 块。
- **坑①复盘**：本会话严格「一回合一个 Edit + 下一回合 grep 复核」，三次编辑（`__init__` import 块、`__all__` 整块、docstring）全部一次落盘成功——纪律有效。
- **解锁**：M2-B（04+05）全部完成 → M2 仅剩 M2-C：06（false_positive 场景 golden 数据，`ready-for-human`，涉 D-12/D-18 契约面）+ 07（端到端验证，需用户确认真实 LLM API key）。

## issue 06 派工提示词生成（未执行，供新会话使用）

- 核实票面与契约面后发现两处票面未写透的张力，已写入提示词要求执行会话先红后改：① R2 成对规则（dev+holdout 双集强制）与 D-21「holdout 同步延至 M7 前 + 本票 holdout 零改动」冲突 → 推荐过渡豁免常量 `HOLDOUT_SYNC_PENDING` + decisions.md D-21 补记；② `ScenarioCategory` 六取值冻结无误报类目 → 推荐扩「误报类」+ 覆盖测试 6→7。
- 另核实：few-shot 侧已预埋（VALIDATION_SCENARIO_SLUGS 含 false-positive-flap、loader 已读可选 classification）；rules.yml 8 条规则均带 scenario label 与剧本双向对应，新剧本需增第 9 条专属规则（labels.scenario=false-positive-flap）供 inject 下调阈值。

## M2-C issue 06（T6 · 误报剧本 false-positive-flap + golden 标注扩展）——resolved @ 本会话

- **落点**：`schema.py` 三处（AlertEvent 可选 classification 缺省 incident / ScenarioCategory 扩「误报类」/ HOLDOUT_SYNC_PENDING 过渡豁免常量 + R2/R3/R6 豁免路径）+ `tests` 4 文件 12 新例 + `chaos/scenarios/12-false-positive-flap/` 三件套 + `rules.yml` 第 9 条专属规则 DemoTasksLatencyFlap + `datasets/golden/dev/false-positive-flap.yaml`（2 runs dump 实测）+ issue 06 resolved + decisions.md D-21 落地补记 + M7 前待办行。
- **先红后改**：4 schema 红测试（含缺省字段访问 AttributeError 型）+ golden_tree 导入红，全数转绿；既有 11 剧本零改动回归绿（缺省 classification 向后兼容机械证明）。
- **实测基线（关键弯路）**：64 连接 demo-load 并非"健康水位"——P95≈0.87s 压出 3 条真实告警，且灌 3 万任务致 broker 积压 2.6 万、stale_pending 残留 firing。整备（清 broker + pending 补偿 failed）后用自建 2/s 轻探针重建健康基线：P95 稳定 24.6–24.9ms（25ms 桶）、零 firing。**教训：基线探针强度必须低于 worker 消费能力（~10/s），"健康水位"要用 firing=NONE 实证，不能照抄注释口径。**
- **阈值定案**：默认 0.12s（避开 DemoHighErrorRate 的 0.1 保证 sed 唯一命中）/ 注入 0.01s；reload 走 SIGHUP（未开 --web.enable-lifecycle，/-/reload 不可用）；inject/cleanup 备份-还原 + 双幂等兜底。
- **门禁**：262 passed / 4 skipped（净增 12）、coverage 97.29%、ruff 双绿；schema.py 压注释到 299 行守 C6 ≤300。
- **新坑⑩**：`pytest ... | grep` 会吞 pytest 退出码（管道以 grep 状态收尾）——门禁必须 `> file; PYEXIT=$?` 或先落盘再 grep，本次抓到一次假绿（301 行超 C6 但显示全绿）。
- **解锁**：M2-C 仅剩 07（3 剧本端到端验证）——需用户确认真实 LLM API key（DeepSeek/Qwen）+ 9 容器栈常驻；holdout 同步 + 清空 HOLDOUT_SYNC_PENDING 已挂 decisions.md M7 前待办。
