# 2026-09-09

- 补落 M4-08 遗留风险清单到 issue 08 Comments（此前只在对话汇报里，未进权威票面）：三条 = Top-1 复核口径 / 同参绕圈三候选 / holdout 同步豁免挂起；提交 `ca4302e`。M7 规划以该处为单一权威来源。
- 生成 M5 设计文档派工 prompt：`.scratch/tmp/m5-design-dispatch-prompt.md`（设计三段式：起草→G 表评审→定案拆票；零写码设计期口径；权威口径已核对 PRD §7-M5 / roadmap / execute_action L2 stub / D-39 起编号）。
- 按派工票执行 M5 设计起草（零写码）：产出 `docs/design/m5-remediation-gates-design.md`（draft，217 行，G1–G10 + R1–R5）。核心推荐主线 = execute_action 实装为干跑模式（调查内只出干跑提案，G1）+ confirm API 同步执行 + remediation 服务确定性走完四道闸门；C3 论证关键发现 = pyproject C3 禁列已预置 `oncall.remediation`，handler 沿用 registry 注入接缝（TYPE_CHECKING/Protocol，与 Fetcher 同构）；白名单 = 静态原子操作表（docker/mysql 两族）；恢复判据 = runbook verification 显式声明。外部依据：Claude Code permissions / Anthropic containment（93% 批准疲劳）/ Google SRE On-Call / OWASP 命令注入（全 URL + 取用 2026-09-09）。评审前门禁全绿 541/10 + ruff 双检。draft 待提交 `docs(M5-处置闸门)`，G 表交用户逐条拍板（D-39 起登记、拆票在定案后）。
- 执行 M5 issue 07（T7 验收断言与门禁，mock e2e 收口）：新增 4 个 e2e 断言文件（acceptance_gates 300 行 / frozen_face 56 / command_gates 73 / mock_e2e 225，全 ≤300），实测全量 **679 passed / 10 skipped**（667 基线 + 净增 12）+ ruff 双检 + import-linter + bandit 全绿。**契约缺漏修正（用户拍板）**：cpu-spike runbook 第 3 步 restore_cpuset 原缺 `cores`，与 issue 05 白名单 `{container, cores}` 冲突——runbook 补 `cores: $cpuset_cores` 运行时模板（$var 执行期注入语义），`test_execute_action_dryrun` 渲染断言同步更新。escalated 未恢复侧与 mock e2e 重复故合并（File A 管恢复侧、File B 管未恢复侧）。回写完成：issue 07 → resolved + 验收 4 条勾 + 落位注记；设计文档验收节 mock 可判定 6 条勾选（T8 两条不勾）；spec.md 07 行 resolved。**待用户确认后入库**（单 feature commit `test(M5-处置闸门)` + Closes issue 07 路径）。
- M5 G1–G10 评审定案（用户「可以」全按推荐解）+ 评审后动作闭环：设计文档翻 reviewed + decisions 登记 D-39–D-48 + CONTEXT 补 6 术语（处置提案/干跑预览/命令白名单/恢复判据/受控执行）+ docs/README 刷新——提交 `9e83f85`；拆票 `.scratch/m5-remediation-gates/` spec + issues/01–08（T1–T7 ready-for-agent、T8 附 demo 栈门槛）——提交 `4b75cb1`。T 表阻塞链 01→02→03→{04,05}→06→07→08。架构回写预告：§4 七表→八表随 T3、§3.3 L2 注记随 T2、§5 时序核对随 T8。前沿 = issue 01（runbook 契约+解析器）。
- 生成 M5 issue 01 派工 prompt：`.scratch/tmp/m5-01-dispatch-prompt.md`（T1/G5/D-43，runbook 契约+解析器）。起草时核查素材实况：cpu-spike cleanup = docker 停容器+恢复 cpuset、slow-sql cleanup = mysql KILL 持锁会话；golden `remediation` 字段 = P95≤0.05s / 连接池回落≤35s 排空。发现并裁决三处落位点：① pyproject 已含 pyyaml（M0 scenarios 在用）→ D-43「自写 frontmatter 切分不引 PyYAML」偏离为复用 yaml.safe_load（C2 零新依赖字面满足，留痕待用户确认补记）；② allowlist 模块在 issue 05 才建 → runbook.py 内置最小「动作族注册表」常量（05 票 import 扩展），解析期校验动作引用存在性；③ rollback 显式空 `[]` = 无回滚预案、验证失败直接转人工（cpu-spike 处置即恢复）。
- 按派工票执行 M5 issue 01（runbook 契约+解析器，T1/G5/D-43）：新建 `remediation/runbooks/cpu-spike.md`+`slow-sql.md`（仓根库）、`src/oncall/remediation/{__init__,runbook}.py`（解析器，runbook.py 211 行，C6≤300）、`tests/unit/test_remediation_runbook.py`（23 用例）。TDD 全绿一次过。契约落地：frontmatter 六字段 + step={action:"ns.op",params}（$var=运行时变量仅结构校验）；ACTION_KEYS=Final frozenset{docker.remove_container,restore_cpuset,mysql.kill_session}（裁决②落位,C8 合规）；rollback 显式空合法(cpu-spike)/非空幂等重试(slow-sql,D-45)；verification PromQL 照抄 rules.yml（histogram_quantile P95 / avg_over_time demo_db_pool_used）+ 库加载 fail-closed。三裁决①②③ 已写 issue Comments + 收尾汇报待用户确认（①偏离 D-43 复用 pyyaml 见上文，②③同）。门禁 564/10（基线 541+23）+ ruff 双检 + import-linter 全绿。issue 01 Status→resolved、spec 01 行勾选。
- 用户采纳 M5 issue 01 裁决①（复用既有 pyyaml）：补记一句到 `docs/design/decisions.md` D-43 理由栏（「frontmatter 定界切分自写 ≈30 行；YAML 本体复用既有 pyyaml，C2 指零新依赖非禁既有依赖」），决策结论不动。未提交（随下次该文件提交或单独 commit）。
- 生成 M5 issue 02 派工 prompt：`.scratch/tmp/m5-02-dispatch-prompt.md`（T2/G1+G3/D-39/D-41，execute_action 干跑实装 + L2 授权判定器）。起草时读 harness 现状源码厘清注入面：loop.py `LoopComponents{registry,gate}` → `_execute_step` 先 `gate.check` ALLOWED 才 `registry.execute`；schemas `ExecuteActionInput{action,params}` + `ToolResult` 冻结；permission L2=DENIED。**核心安全面裁决①**：`test_harness_loop_mechanisms.py::test_fallback_on_gate_denial` 现驱动 execute_action 断言 L2 拒绝——D-39/D-41 已拍板「循环内 L2 只出干跑」（干跑无 demo 副作用故 loop 内 ALLOWED 放行到干跑 handler，真实执行走 03/04/05 不经 loop），该测试+`test_l2_always_denied`+`test_execute_action_stub_is_l2_defense` 须更新为新干跑语义（有意后果非回归）。prompt 已要求收尾逐条列给用户确认。基线 564/10（含 issue 01 的 23 用例）。D-43 采纳 + issue-02 prompt 均未提交（.scratch/tmp 与 decisions 改动待用户决定随下次 feature/单独 commit）。
- 执行 M5 issue 02（execute_action 干跑实装 + PermissionGate L2 授权判定器，T2/G1+G3/D-39/D-41）：实现裁决①循环内干跑语义。新增 `src/oncall/harness/tools/execute.py`（197 行）：`build_execute_action_handler(*, runbook_loader, proposal_creator)` 干跑 handler——解析 action 定位器 → 注入 loader 定位 Runbook → 渲染「白名单原子操作+参数模板」命令清单+影响面（$var 进 runtime_params 显式标注）→ proposal 存根接缝产 proposal_id → `ToolResult{ok, dry_run_preview+proposal_id}`；零 subprocess/零 demo 写/C3 零 remediation import（类型仅本地结构 Protocol）。registry 注入面复用既有 `handlers` dict 按名注入；未注入维持 `execute_action_stub`（reason 更新「处置执行器未装配」）。permission.py：`PermissionGate` 增可注入 `l2_judge`（处置授权判定器，D-41），默认 `_loop_l2_dryrun_allow` 对循环内 L2 execute_action 干跑请求 ALLOWED，`LEVEL_DECISIONS[L2]` 由「永远禁止」改注记；gate.check 仍纯函数。裁决① 更新的既有测试（有意后果非回归）：`test_l2_always_denied`→干跑 ALLOW、`test_l2_denial_audited`→ALLOW 审计、`test_execute_action_stub_is_l2_defense`→拆未装配 stub+注入干跑两测、`test_fallback_on_gate_denial`→拆干跑落证据步+判定器拒绝 Fallback 两测。架构 §3.3→§3.4 L2 语义回写（永远禁止→循环内干跑/确认后服务层执行）、CONTEXT 补「推理与执行分离」术语。测试：新 `test_execute_action_dryrun.py`（13 测）+ 更新 3 个既有测试文件。门禁 564→580 passed/10 skipped + ruff 双检 + import-linter C3 + 架构守卫全绿。issue 02 Status→resolved、spec 02 行勾选。未提交；decisions.md D-43 采纳注记仍悬（issue 01 遗留，待用户决定随本提交或单独 commit）。
- 用户确认采纳 issue 02 裁决①并要求代为提交 → 核查发现 issue 02 已由执行会话自行入库：`a2d1369 feat(M5-处置闸门): execute_action 干跑 handler + PermissionGate L2 授权判定器（issue 02）`（含 decisions.md D-43 采纳补记 2 行，已核实）+ `914fdc6 chore(worklog)`（memory 回补）。基线 = 914fdc6，工作区干净，全量 580/10 复核通过。无需再提交。
- 生成 M5 issue 03 派工 prompt：`.scratch/tmp/m5-03-dispatch-prompt.md`（T3/G8/D-46，remediation_proposals 第八表 + service.py 状态机）。起草前实读：issue 03 票面（七验收含 dry_run_json 不可变/investigation_id 可空）、design G2/G8/数据模型节/C3 依赖线、decisions D-46/D-40/D-30/D-19/D-25/D-31、architecture §4（现 7 行 + 「六张核心表」标题过时）、models.py 202 行 + db/__init__ create_tables 幂等、execute.py ProposalCreator 接缝（payload 3 键→str，service 需填）、ingest/service.py 函数式服务先例、test_db_evidence_* 的 in-memory sqlite+PRAGMA FK 装配先例。预铺裁决：approved 态=04 同步链中「人点头已落库」持久标记（瞬态也须合法）、迁移表需含回滚空 runbook 未恢复直转 escalated 边（06 语义 D-44/D-45）、C8 用 Final AnnAssign、DB CHECK 只锁值集迁移规则只在 service 层、基线数字票面 541 旧值以 580 为准。prompt 含 0 环境/1 必读/2 任务(A-E)/3 边界/4 裁决踩坑/5 验证清单，要求收尾把迁移表定稿等列用户确认后入库。

## M5 issue 03（T3/G8/D-46）proposal 落库与状态机 — 已实现待入库
- 第八表 remediation_proposals：models.py 202→256 行，16 列照 D-46；incident_id FK index 不唯一（一对多）、investigation_id FK nullable、status 八值 CHECK + decision 二值 CHECK；未拆 models_remediation.py；db/__init__ 不扩导出
- 状态机 service.py（277 行）：_TRANSITIONS 常量钉死 pending→approved|rejected→executing→recovered/failed/rolled_back/escalated（escalated 直边服务 D-45 回滚空路径）；ProposalStateError；时间戳口径 confirmed_at/executed_at/finished_at；dry_run_json 全路径不写（验收⑤测试钉死）；make_proposal_creator 对账 issue 02 ProposalCreator 接缝（C3 零边）
- 必要适配：test_db_evidence_models.py + test_db_alert_events.py 表清单断言 5→6 表（机械更新）；harness 行为测试零改动
- 回写：架构 §4 六→八张核心表 + 第八表行；issue 03 票 resolved + 验收打勾 + 落位注记；spec.md 03 行 resolved；CONTEXT/decisions 未动（无新共享概念、不新增 D）
- 门禁：全量 609 passed / 10 skipped（基线 580+29 新增）；ruff check/format 双绿；架构守卫 7 passed
- 坑重演：同文件并行 Edit 丢改动（noqa 注释三处全丢）——串行重做后恢复；测试侧三处缺 approve 步导致假红
- 待用户确认迁移表定稿/approved 语义/回写位置后入一个 feature commit（Closes issue 03 文件路径）

## M5 issue 03 入库 + issue 04 派工 prompt 生成
- 03 feature commit `ecd6e99`（10 files, +909/−15），工作区恢复干净（仅例外路径）
- 04 派工 prompt 落 `.scratch/tmp/m5-04-dispatch-prompt.md`：基线 ecd6e99、门禁 609/10、注入面 Protocol 建议住 remediation 侧、D-48 降级 503 + approved 即终、状态判断只在 remediation 层（api 捕 ProposalStateError→409）

## M5 issue 04 — 确认门 API（T4 / G2 / D-40 / D-48）
- 基线 ecd6e99，TDD：先 tests/unit/test_remediation_api.py（12 用例）跑红 → 实现跑绿。
- 新文件：src/oncall/api/remediation.py（139 行，三端点 + RemediationDeps + ConfirmRequest）；src/oncall/remediation/executor.py（RemediationExecutor Protocol）与 verifier.py（RecoveryVerifier Protocol）——真实实现归 05/06。
- 关键裁决：approve 先落库再判降级（终态再 confirm 恒 409，降级 503 时 proposal 留 approved）；执行输出摘要落 params_json["execution"]；执行器/验证器唯一输入 = dry_run_json 原文（deep-equal 断言）；未恢复本票落 failed（回滚/转人工分叉归 06）。
- app.py 挂载 create_remediation_router，缺省 RemediationDeps() 即降级形态。
- 门禁实测：621 passed / 10 skipped（基线 609/10 + 12），ruff 双检绿，架构守卫 7 passed。
- 回写：issue 04 → resolved + 落位注记、spec.md 04 行 resolved；architecture.md 无 API 清单节未动（待用户拍板是否补）。
- 状态：已汇报，等用户确认后一个 feature commit 入库。
- 用户确认后入库：58ce4e8 feat(M5-处置闸门) issue 04（7 files, +555/-14）。已备好下一票派工 prompt：.scratch/tmp/m5-05-dispatch-prompt.md（issue 05 命令白名单+受控执行器，基线 58ce4e8 / 621 passed / 10 skipped）。

## M5 issue 05 执行（命令白名单 + 受控执行器，T5/G4/D-42）
- 新增 `src/oncall/remediation/allowlist.py`（88 行，静态原子操作表 + render_argv）与 `executor.py::ControlledExecutor`（159 行含 Protocol，runner/runtime_values 注入、30s 超时、4KB 限长、执行审计）。
- dry_run_json commands[] 增补 `params` 键（execute.py 纯增量）；pyproject bandit 豁免 B404/B603（三重看管：白名单渲染 + shell=False + test_no_shell_true AST 守卫）。
- 门禁实测 649 passed / 10 skipped（621+28），ruff 双检绿，bandit 0 issue；票面/spec 回填 resolved，待用户确认后 commit。
- issue 05 入库 commit `0d7c504`；新派工 prompt 已写 `.scratch/tmp/m5-06-dispatch-prompt.md`（基线 HEAD 0d7c504、门禁基线 649/10）。

## M5 issue 06（恢复验证 + 回滚）执行完成，待确认入库
- `RunbookRecoveryVerifier` + 回滚编排 `run_confirm_chain` 同文件落 `src/oncall/remediation/verifier.py`（216 行；service.py 278 行逼近 C6 ≤300 上限，编排落 verifier 模块）
- 判定形状：`{recovered, promql, condition, window_s, observed, samples}`，异常路径 +`error` fail-closed；condition 形状定稿 = 观测名 op (数值|参考名)，观察窗全样本满足才判恢复
- `rollback_status` 三值定稿：skipped / rolled_back / blocked（白名单全拒 → escalated 兜底不复验）
- api：`RemediationDeps` 增 runbooks，confirm 链换单调用点；未恢复落点 failed→escalated（D-28），对应 api 测试改名对齐；StubExecutor 对齐 issue 05 审计形状
- CONTEXT.md 新词：回滚编排 / 回滚；票 06 + spec 06 行已回填 resolved
- 门禁实测：667 passed / 10 skipped（649 基线 + 18 新增），ruff 双检 / import-linter / bandit / 架构守卫全绿
- 状态：**未 commit**——按派工 prompt 待用户确认判定形状 / rollback_status / 编排落点 / 验收计数后再入库

## M5 issue 06 入库 + issue 07 派工 prompt 就绪
- issue 06 以 `f7f4c26` 单 commit 入库（7 files，+740/-43），Closes 票面路径，`.workbuddy/` 与 `.scratch/tmp/` 未卷入
- issue 07（验收断言与门禁 T7）派工 prompt 已写 `.scratch/tmp/m5-07-dispatch-prompt.md`：基线 HEAD `f7f4c26`、门禁基线修正 541→667/10、断言落 `tests/e2e/test_m5_acceptance_gates.py` + mock e2e `test_m5_mock_e2e.py`（M4-T7 形制先例），T8 两条不勾、活栈归 08
- M5 issue 07（T7 验收断言与 mock e2e）完成并入库 f9c064a：4 个 e2e 断言文件（acceptance_gates/command_gates/frozen_face/mock_e2e，均≤300 行）+ cpu-spike runbook 补 cores:$cpuset_cores（契约缺漏经用户拍板）。门禁 pytest 679/10、ruff 双检、import-linter、bandit 全绿；设计文档验收节勾 6/8（T8 两条不勾）。M5 tracker 01–07 全 resolved，剩 T8 真实 e2e（活 demo 栈）。

## M5 issue 08 真实 e2e 与收官（T8，2026-09-09）

- 活栈 2 剧本真实 e2e 全绿：cpu-spike recovered（121.9s/3 命令/恢复窗 120s/P95 0.045）、slow-sql recovered（106.1s/2 命令/105s/used 2.5<5）；设计文档翻 implemented，M5 8/8 resolved 收官。
- 两个 mock 层测不出的真实发现：① allowlist mysql.kill_session 缺 -poncall（Access denied，KILL 失败但池排空仍判 recovered 的次生风险）；② processlist 持锁会话发现 SQL 自匹配（需 id != CONNECTION_ID()）。
- 观察窗语义：执行后立即回查必含故障样本 → harness 侧 PollingRecoveryVerifier 轮询（15s/240s）直到 60s 窗口干净，生产代码零改动。
- 门禁：682 passed/12 skipped + coverage 97.75% 全绿；import-linter C3 KEPT 但 C4+C5 在 grimp 3.17 组合下工具异常（环境漂移，AST 守卫同等覆盖），建议后续 pin grimp<3。
- 提交：f1a5495（fix 白名单密码）/ e9b16c5（test 真实 e2e harness）/ 5e87e54（docs 回填收官）。

## M6 设计草案（~13:20）
- 产出 docs/design/m6-report-kb-design.md（status: draft，G1–G9 待评审），形制照 M5；提交 ff99659。
- 核心取舍：闭环报告读库拼装不落表；kb_chunks 第九表为权威 + Chroma 可重建索引；embedding 拟选本地 bge-small-zh-v1.5（新增依赖走评审）；开局确定性召回（非 Agent 步）+ query_kb Planner 自主；知识污染三道防线；缓存边界 = 指纹精确命中 + mitigated 实证才复用。
- 新术语入 CONTEXT.md（闭环报告/知识块/开局召回）；docs/README 索引更新；门禁 pytest 682/12 持平 + ruff 全绿。
- 待办：用户评审 G1–G9 → 登记 D-49+ → 建 .scratch/m6-report-kb/ tracker（T1–T7 预告）。

## M6 执行收官（~14:30）
- 评审定案：G1–G9 全采纳 → D-49–D-57 登记；tracker `.scratch/m6-report-kb/`（issues 01–06 resolved，07 ready-for-agent 环境门槛票）。
- 提交链：8be5769（定案）→ 8e02d83（T1 第九表）→ 77a1089（.scratch/tmp 误提交修复，已 gitignore）→ a02e6f5（T2 闭环报告）→ b3adc4c（T3 切块/向量/管线）→ e669eda（T4 query_kb+召回）→ f31b322（T5 污染防线②）→ cd04f79（T6 架构回写）。
- 关键实现：kb_chunks 第九表；knowledge 模块（report/chunking/embedder/store/pipeline/retriever/recall）；query_kb 实装 + 开局召回两通道（canonical 子集缓存复用 / 向量参考）；Verifier 校验5 kb-only 证实拦截；app.py env 门控 ONCALL_KB_ENABLED（缺省关）。
- 执行修正：指纹跨事件命中实为 canonical label 子集（D-14 全指纹含桶唯一约束）；.scratch/tmp 不进版本库教训（git add -A 误带，已 gitignore）。
- 门禁：pytest 710/12（+28）+ ruff 双检 + bandit 全绿；依赖冒烟 chromadb+sentence-transformers 装包成功。
- 待办：T7 真实召回 e2e（需用户确认活 demo 栈 + bge 模型首载）；M7 评测台注意 skip_cache 旁路。

## M6-T7 真实召回 e2e（commit be4dc97，issue 07 resolved）
- 活栈 slow-sql 注入两遍：第二遍 reused_from=11（0.2s）、向量召回 Top-3 0.68/0.63/0.63 / 0.151s；设计文档翻 implemented。
- **修复 T4 装配顺序缺陷**：create_app 调查路由注册先于 kb wiring → 闭包捕获旧 deps（kb_retriever=None）→ 接线面开局召回失明；wiring 提前 + 回归测试（kb_pipeline 接线面）。
- /investigate 500 兜底补 logger.exception；遗留：confirm 长事务内 D-56 自动入库偶发失败（疑似 sqlite 锁竞争），建议后续挪出长事务。
- 模型首载坑：huggingface_hub 经本机代理/镜像组合持续 0 字节 → requests 手工落盘 `~/.cache/oncall-models/bge-small-zh-v1.5`（ONCALL_BGE_MODEL_DIR 可覆盖）。
- 门禁 711 passed / 13 skipped；e2e 开关 ONCALL_RUN_KB_E2E=1，测试文件 tests/integration/test_m6_real_recall_e2e.py。

## M7 评测台设计票（2026-09-09 下午）

- 按派发 prompt 完成 M7 设计草案：`docs/design/m7-eval-bench-design.md`（draft），G1–G8 决策点预填推荐解（进程内 runner / 两级判对错 / N=3±极差 / 第十表 eval_runs / 模型 profile env 参数化等），待用户逐条拍板后登记 D-58+。
- 建 `.scratch/m7-eval-bench/` tracker（spec + issues 01–07，全 needs-triage，评审前冻结派工）；CONTEXT.md 评测节新增「评测运行 / 判对错两级 / 复用命中率」。
- 提交 `dc5c177`（docs(M7-评测台)）。关键设计取舍：复用出口不计命中分母（D-57 延伸）、escalated 不计失败（D-56）、judge 与被评模型解耦、真实评测走 key 门槛票（D-50 先例）。

## ⚠️ .git 对象库损毁事故（2026-09-09 17:39）

- T1 提交时发现 `.git/objects` 全部 pack 丢失（仅剩 .idx + 17 松散对象），HEAD（cf87279，今日评审 commit）不可达；GitHub 代理 502 无法从远端恢复。
- 处置：旧 .git 原样挪到 `.git-broken-20260909/`（未删除）；`git init` 重建 + 全工作树基线提交 `e8241f9` + .gitignore 排除取证目录 `2b1e1db`。旧历史（D-01..D-57 各 commit）本地对象已失，GitHub 远端 `HLGZJY/oncall-copilot` 若含旧历史需用户拍板对齐方式（force-push 或归档）。
- T1 成果全部保留在工作树与基线提交内：eval_runs 第十表（db/eval_models.py）、golden 三守卫、架构 §4 回写、门禁 727 passed。仓库外另有今日 17:33 纯工作树备份 `oncall-copilot-backup-20260909`。
- 教训：commit 后立即 push 是唯一防损手段；本地对象库可在无征兆下被外部进程清空（嫌疑：同步/清理工具，时间点 17:33-17:39）。

## ✅ 事故恢复完成（2026-09-09 18:15–18:30）

- 关键发现：损毁 .git 的**松散对象**中保留了近期全部 commit 链——拷回新 .git 后 `cf87279` 的 116 个提交完整连通到仓库起点，且 merge-base 验证远端 `e24c2c3`（M4 T7）正是其祖先。
- 处置：`git reset cf87279`（mixed，工作树无损）回到真实历史；T1 重做成规范提交 `3d92366` + M6 T7 漏提交的编码修复 `e4a05c3`。**无需 force-push**（远端是祖先，普通 push 即 fast-forward）。
- remote origin 已重新配置（新 .git 不含旧 config）；push 因代理 502 间歇失败待重试——**网络恢复后第一件事 push**。
- `.git-broken-20260909/` 保留作取证（已被 .gitignore 排除）。
- 待办：① push 后核对远端 HEAD；② 排查清空 pack 的外部进程（时间窗 17:33–17:39）；③ 建议 commit 后立即 push 的习惯固化。

## 清理（2026-09-09 18:35，用户确认 A+B）

- 删：mypy/pytest/ruff/grimp/import_linter 缓存 + .coverage + 22 个 __pycache__（179 pyc）+ junit_tmp.xml / result-tmp.xml / ingest-itest.db-journal，共约 16.5MB；删后 pytest 全量 727 passed 验证无损。
- 保留并 gitignore：`.oncall-copilot-readtrack/`（readtrack 笔记项目工作区，用户拍板不入库）；`oncall.db.bak-20260908` 与 `.git-broken-20260909/` 未获指令继续保留。
- 提交 `116ce07`。仍未 push（代理 502），待网络恢复。

## issue 02 派发 prompt（2026-09-09 20:05）

- 产出 `.scratch/m7-eval-bench/dispatch-02.md`（自包含：环境绝对路径 / 必读清单 / TDD 纪律 / 门禁基线 727 passed / 验收 / 输出格式），供新会话直接粘贴执行。
- 当前状态：issue 01 已完成（3d92366），issue 02–06 ready-for-agent；push 仍因代理 502 未成功，本地领先远端。

## M7 issue 02 剧本 runner（2026-09-09 晚）
- `src/oncall/eval/runner.py` 新增：run_scenario 对单剧本 N=3 遍装配既有 LoopComponents 直跑 run_investigation，明细落 eval_runs；ScenarioCase(golden+spec) 成对注入、judger 判定接缝（issue 03 实现）、require_real_tier 真实档 ONCALL_RUN_M7_EVAL=1 门槛、unstable 单列（D-61）。
- 关键教训：mock 收束剧本零证据步直接给结论会被规则层判 hallucination（"零证据步即收束结论"）——测试剧本须先取证一步再收束。
- 顺手修 issue 01 遗留 test_db_eval_runs 两处 RUF100。
- 门禁：全量 748 collected（基线 740+8）exit 0；ruff 双检 0 错。commit `7131043` 已推送 origin main。issue 02 状态 → resolved。
- 下一步：issue 03 判对错管线（派发 prompt 草稿已给用户）。

## M7 issue 03 判对错管线（T3）完成
- 提交 `0f79601`：feat(M7-评测台) 判对错管线——规则匹配层 + judge 契约 mock 冻结
- 新增 `src/oncall/eval/judging.py`（157 行）：`rule_judge`（normalize_hypothesis_text 规范化包含，结论命中 top1 / 假设命中 top3 / miss，judged_by=rule，直接实现 issue 02 Judger 接缝）；judge 契约 `JudgeInput`/`JudgeOutput`/`Judge` Protocol + `MockJudge`（畸形输出抛 LLMOutputError，逐字对齐 D-07）；防自评常量 `JUDGE_ENV_PREFIX=ONCALL_JUDGE_LLM`；`judger_from_judge` 适配器（judged_by=judge）
- 新增 `tests/unit/test_eval_judging.py` 13 测：top1/规范化包含 top3/miss 边界、mock 契约与畸形输出、接缝连通（rule_judge 直接传 run_scenario）
- 门禁实测：pytest 761 collected（748 基线 +13，全绿）；ruff check + format 0 错；单文件 ≤300 达标
- 教训复现：同文件并行多 Edit 丢改动（测试文件 import 块与 judging.py 适配器各丢一次），改串行后恢复——与既有经验一致
- `git push origin main` 失败（CONNECT tunnel 502，代理网络问题），commit 已在本地 main，待网络恢复补推
- issue `03-judging-pipeline.md` Status → resolved；下一步 issue 04（指标核算 metrics）

## M7 issue 04 指标核算完成（2026-09-09）
- 新增 `src/oncall/eval/metrics.py`（125 行）：`compute_run_metrics`（五列矩阵，eligible 分母排除 reused/escalated，均值±极差排除 reused 行）、`classify_failure_mode`（多数归类→unknown 禁丢弃 + requires_human 旗标）、`summarize_runs`（剧本×模型×集合分组，unstable 单列）。
- 新增 `tests/unit/test_eval_metrics.py`（164 行，11 测试）：命中/复用/escalated 混合、不除零（空集/全复用/全 escalated）、均值极差、多数归类、unknown 人工通道、组内 unstable。
- 门禁：772 collected（基线 761 + 11）、pytest 全绿、ruff 双检 0 错、C6 达标。
- 提交：b0a00c0（feat）+ 73218c7（issue 状态 resolved）。push 失败（代理 502 / schannel 断连），不阻塞，待重推。

## M7 issue 05 完成（2026-09-09 晚）
- `63c3bb4` feat(M7-评测台): 矩阵跑法 + 模型档案 env 参数化 + 报告双产物
- 新增 `src/oncall/eval/report.py`（201 行）：`ModelProfile`（env 前缀 `ONCALL_LLM_PROFILE_<NAME>_*`，尾词复用 ONCALL_LLM_* 切换面词汇）、`resolve_profile`（复用 LLMClientConfig.from_env，零硬编码模型名）、`run_matrix`（profile×剧本×N=3，委托 run_scenario 实跑，flush 落 id）、`build_matrix_report`（消费 summarize_runs，每组带 run_ids 可回溯 eval_runs 行）、`render_markdown`（矩阵表 + 选型理由骨架）、`write_report`（runs-<date>.json + report-<date>.md 双产物，G5 落 datasets/eval/）
- 新增 `tests/unit/test_eval_report.py` 13 测：矩阵形状 2×2×3、run_ids 抽验、零硬编码模型名机械检查、profile 缺项 fail-fast
- 门禁：pytest 785 collected（772 基线 + 13）全绿；ruff 双检 0 错；CONTEXT.md 增「模型档案 / Model Profile」词条；issue 05 → resolved
- 教训：同文件并行多 Edit 会丢改动（本会话两次中招），同文件编辑须串行
- push 成功（b6effe1..63c3bb4，此前 502 卡住的三笔补交一并上去）
- 下一步：issue 06 派发

## M7 issue 06（Makefile/CI 回归收口 + 架构回写 + 文档）— f502778
- 新增 `src/oncall/eval/entry.py`：`run_mock_eval`（2 profile × 12 dev × N=3 矩阵，全 mock 零真实 API，复用 run_matrix/write_report/rule_judge）+ `run_eval_real`（复用 require_real_tier 门槛，deferred 不真跑，归 issue 07）+ CLI `python -m oncall.eval.entry [mock|real]`。
- Makefile 加 `eval` / `eval-real`；CI eval-regression 占位命令替换为真实入口（防假绿）。
- 架构 §4 删除旧规划版 eval_runs 残留行，第十表与 eval_models.py schema 逐列一致；CONTEXT.md 补「判定不一致 / Unstable」（D-61）；README 增评测章节占位（挂图归 M8）。
- 门禁：pytest 通过，collect 785→794（+9，tests/unit/test_eval_entry.py）；ruff 双检 0 错；entry.py 174 行。
- 踩坑：①NotImplementedError 继承 RuntimeError，main 的 except 分支需先判 env 再捕 deferred；②同文件并行多次 Edit 会丢改动，改串行整文件重写。push 代理 502 未上远端（本地 commit f502778）。

## issue 07（M7 真实档端到端跑批 + 档位回填）完成

- 提交链：8882ebb（run_eval_real 实现）→ 785ea81（档位终选 k2.6+k3、judge=k2.7-code + judgment.reason 持久化 + profile 前缀连字符等价 + 单测触网修复）→ 45f0e56（单格异常隔离 + 逐 case flush）→ 49bbc6d（72 格实测回填收尾，issue → resolved）。已 push 远端（ls-remote 核实 HEAD 一致）。
- 模型现役实况：key /models 仅 k2.6 / k2.7-code / k2.7-code-highspeed / k3；k2.5 与 moonshot-v1 系已不可调（D-66 两轮复核）。
- 最终批3 实测（72 格，2h01m）：全 miss、0 unstable；failure_mode = tool_error 54（M6-T5 防线拦截 KB 支撑）+ plan_error 9 + timeout 8；成本 k2-6 ¥0.125/次（均值 100.8s）｜k3 ¥0.282/次（81.0s）｜judge ¥0.152/18 次；累计 spend ≈ ¥24 < ¥36 硬顶。
- 关键教训：①planner 默认 30s 超时对推理型模型过紧（TIMEOUT_SECONDS 90/180/60 修复）；②共享 judge client 的累计 usage 快照逐行复制会虚增 N 倍，judge 用量只许批级聚合；③M6-T5 VerifierError 穿透评测装配面炸批 → run_scenario 单格 try/except 记 error 行；④run_eval_real 内部 load_env_file 会把本机 .env 补回，单测删 env 无效——测试须把 ONCALL_M7_ENV_FILE 指向空文件防触网（A1）。
- 实测结论：当前 harness 证据面 + 防线交互下两档模型均无产出，「真实 Planner 调查视图缺事件锚点」（M5 issue 08 注记）升为 harness 缺口票首要嫌疑。
- 待办：20% 人工抽检（judged_by=judge 18 行全量 + rule 随机 14 行）回填 issue Comments；M7 收尾票（README 挂图 + 评测叙事沉淀）。
