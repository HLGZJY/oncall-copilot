# 2026-09-06

- 创建 `AGENTS.md`（约 80 行）：项目简介 + 快速导航（含 ⏳ 占位文件：CONTEXT.md / docs/adr/ / .scratch/ / docs/agents/，惰性创建后回填）+ 12 条硬性规则 + 提交规范。
- 用户反馈后重写 `AGENTS.md`：**以 `项目信息.md` 为主要蓝本**——简介纳入 L0–L5 目标水位（L3）、技术栈地图、验收硬口径；快速导航改为按 项目信息.md 章节索引（模块 A–F / 6.3–6.5 / 附录 B）；12 条硬性规则全部改用 项目信息.md 的核心结论（真 Agent 判据、规则先行 LLM 兜底、白名单三道防线、四道闸门、证据链三必须、评测纪律、成本四杠杆、RAG 定位）。
- 前一日（9-05 会话）结论：资源整理定调——4 份核心文档保留、视频文字稿待归档、`AIOps学习指南.md` 与 `项目背景补充.md` 两份被引用文档缺失待找回；基调 = 文档驱动 agentic engineering + "每个核心模块能用 3 句话讲清"的最低保障线（待补进开发规范）。
- 搭建 `docs/` 结构化知识库（10 份文档，统一 frontmatter 元信息：title/summary/source/status/updated/read_when）：architecture/（tech-stack-map、agent-loop-design）、conventions/（coding-discipline、git-commit-review、security-guardrails）、design/（decisions.md 决策清单 D-01~D-08 + 待定项）、plans/（roadmap-m0-m9、m0-execution）、reference/（glossary-resources、interview-narrative）+ docs/README.md 索引与维护纪律。ADR 惰性建 `docs/design/adr/`。AGENTS.md 导航已挂 docs/。
- 多来源调研后产出两份权威文档：`docs/architecture/architecture.md`（架构文档：10 来源取舍表——Anthropic 5 篇工程博客/Claude Code 源码拆解/SWE-agent ACI/OpenHands SDK 四原则/OpenDerisk；核心取舍：单循环不搞多 Agent 专家团队、上下文重置+JSON 交接优于压缩、推理与权限分离、六组件职责边界、六张核心表、双集评测）+ `docs/conventions/coding-standard.md`（编码规范：8 节——工程约定/目录依赖方向铁律 harness 不 import 业务/LLM 调用与上下文预算硬编码/工具开发 8 条 checklist/测试规范含"测试只增不删"/安全红线 5 条）。docs/README.md 索引已更新。
- 约束层（工程规约与质量门禁）落地，5 个文件：`docs/conventions/quality-gates.md`（调研结论 5 条 + 约束清单 15 条：C1-C9 适配自 Java 参照 + A1-A6 Agent 特有 + 逐项落地方式与豁免流程）、`pyproject.toml`（ruff/mypy strict/pytest/coverage fail_under=80/import-linter 三契约全集中配置，依赖显式版本区间）、`tests/test_architecture_guards.py`（AST 守卫：文件≤300行/禁裸HTTP/禁SDK散落/禁模块级可变全局/prompt 模板化/单测禁真实 SDK，src 不存在时 skip）、`.github/workflows/ci.yml`（五 job：lint/type/test 断网+覆盖率/security/eval-regression 条件触发）、`.pre-commit-config.yaml`（ruff+bandit+gitleaks，rev 锁定）。取舍弃 Checkstyle 式自定义文案 → 需要可操作错误信息的规则全部写成 AST 守卫测试。
- 待办：git init + .gitattributes + 追踪器选型；创建 CONTEXT.md；归档视频文字稿。

## M8 展示层设计文档落地（11:30–11:50）
- 基调确认（用户拍板）：本步仅产出设计文档；选型 Vite+Vue3+Element Plus+ECharts+SSE；P0 六件核心 + P1 三件增强分层。
- 新增 `docs/design/m8-showcase-design.md`：演示故事线、P0 六件（混沌控制台/告警分诊看板/调查工作台/确认门恢复/报告页/回放模式）、P1 三件（Dashboard/评测矩阵/暗色大屏）、API 草案表（标注草案待校准）、mock 策略、验收 checklist、对 roadmap 裁剪顺序的修订建议（P0 信息件不可砍）。
- 更新 `docs/README.md`（快速入口 +1 行）、`docs/design/decisions.md`（新增 D-09 决策条目）。
- 发现：仓库尚未 git init（无 .git），改动未提交；待 issue 追踪器选型后一并处理。

## 开发前仓库体检与文档归位（11:40–12:10）
- **根因定位**：根目录 5 份中文文档性质混杂——`项目信息.md`(683行/51KB，方法论笔记) 与 `数据集介绍.md`(107行) 是外部资料母本；`开发规范与工作流程.md`(692行) 自述"跨项目通用"；`项目介绍.md` 开头是 `### 3.1`、`项目文档.md` 开头是 `## 四、`——**是同一份大文档被切出的两片碎片**。而 `docs/` 下 15 份文件的 frontmatter `source` 字段全部指向这 5 份，即 docs/ 是提炼版、根目录是母本，但 AGENTS.md 导航仍指向根目录旧路径，形成事实上的**双权威**（尤其 `开发规范与工作流程.md` 与 conventions/ 下 3 份 `active` 文件冲突）。
- **用户决策**：只 git init 不提交；规范提到用户级；两份母本下沉 `_sources/`；补建 CONTEXT.md + README.md。
- **执行**：
  1. 整仓备份到 `F:/Git repository/oncall-copilot.bak/`（仓库外，28 文件/265K，未删）；`git init` + `core.autocrlf=false` + `core.quotepath=false`；新建 `.gitignore`、`.gitattributes`（`* text=auto eol=lf`，防 CRLF 踩坑）。
  2. `开发规范与工作流程.md` → `~/.workbuddy/`（跨项目资产），3 份 conventions 的 `source` 回指新位置。
  3. `项目信息.md` + `数据集介绍.md` → `docs/reference/_sources/`，加 `status: archived` frontmatter + "只读不引用"警示块；新建 `_sources/README.md` 说明母本区三条纪律（只读/不新增/不引用）。
  4. `项目介绍.md` + `项目文档.md` → 合并为 `docs/prd.md`（14 节连续编号，完整保留原 §3 闭环图、§4.2 面试官深挖列、§4.4 分步实现——这些在 docs/ 提炼版中**没有**），原件删除。对已提炼部分（排期/简历话术）加"活跃副本见 xxx"指针而非删除，避免信息丢失。
  5. 改写 11 处 `source` 字段（含因 prd.md 重排而需同步的新章节号）+ 12 处内联引用；`AGENTS.md` 导航表拆为"项目内权威"与"方法论母本回溯"两组，保留原有方法论索引但降级为只读。
  6. 新建 `CONTEXT.md`（30 个术语 × 5 类，含 `_Avoid_` 同义词列，划清与 glossary-resources 的"定义 vs 索引"边界）、`README.md`（对外入口，诚实标注 M0 未开工）。
- **验证**：脚本扫 23 个 md / 67 处路径引用，无未处理悬空（剩余 8 处均为有意保留：prd.md 溯源 2 + ADR 模板占位 1 + 外部个人知识库文件 5，已在母本头部加警示说明）。`⏳` 占位全清。
- **坑**：并行对同一 md 文件发多次 Edit 会静默丢改动（工具报成功但未写入），glossary-resources.md 有 2 处因此漏改，靠复核 grep 才发现。同文件多次编辑必须串行。
- **遗留**：`.bak` 备份未删（待用户确认后自行处理）；issue 追踪器选型未定（GitHub Issues vs 本地 `.scratch/`）；`src/` 未建导致 `test_architecture_guards.py` 整组 skip（属 M0 范畴）；`docs/design/adr/` 目录惰性未建。

## 备份删除 + 首次提交 + 追踪器落地（12:00–12:15）
- 用户确认三件事：①删 `.bak` ②首次提交 ③建追踪器 ④更新涉及文档。
- **追踪器方案**：查 `F:\Git repository\skills`（mattpocock 体系）找到官方约定——`.scratch/` 是官方支持的本地 markdown tracker，配套 5 个标准 triage 角色（`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`）。沿用而非自创，保证日后装 skills 零摩擦。
- **`.scratch/` 矛盾**：原 `.gitignore` 把它列入了忽略列表——若用它做 tracker，issue 进不了版本控制就完全失去意义。必须从 `.gitignore` 移除。
- **git 身份坑**：本机全局/系统级 `user.name` `user.email` 均为空（这台机器从没有过提交记录），用户授权后写入全局配置 `HLGZJY <heguojun_0419@qq.com>`。
- **提交粒度**：拆 2 个独立提交而非合并：
  - `2381a59 docs(repo): 归位根目录文档并建立 git 基线`（31 文件 / 2921 行新增）—— 文档归位 + .gitignore + .gitattributes + .workbuddy + 工程配置
  - `31eeb18 chore(tracker): 建立本地 issue 追踪器与 triage 标签词汇`（3 文件）—— `.scratch/README.md` + `docs/agents/issue-tracker.md` + `docs/agents/triage-labels.md`
  - 文件改动存在重叠（AGENTS.md / CONTEXT.md / README.md / docs/README.md 在两个提交里都改了），但用 `git reset` 从暂存区分离 .scratch 与 docs/agents 即可。提交历史因此更精确，日后可单独回退。
- **同步的文档**：AGENTS.md（导航表 + Issue tracker 块 + 提交规范的 issue 引用格式改为文件路径）、CONTEXT.md（新增"六、协作与追踪"6 个术语）、docs/README.md（summary 加 agents + 目录结构加 `agents/` + 维护纪律加一条 `.scratch/` 不忽略的强约束）、docs/design/decisions.md（D-10 决策 + D-11 文档分层 + 两项待定打勾 → 改名为新"issue 升级路径"待定）、README.md（开发纪律加一条）。
- **CRLF 规范化生效**：提交时 git 警告 `数据集介绍.md` 含 CRLF 会被 LF 替换——`.gitattributes` 的 `* text=auto eol=lf` 按预期工作，仓库内统一 LF。
- **第一次提交即生效**：现状工作区干净（`git status --porcelain` 为空），仓库 288K。

## 设计教训（个人级，已写入 ~/.workbuddy/MEMORY.md）
- 「开工前仓库体检」是跨项目可复用的：① 是否真的 git init ② 根目录与 docs/ 是否双权威 ③ CONTEXT/README 缺失 ④ .gitattributes + autocrlf。这四项现在写进 MEMORY.md，下次进任何仓库先扫一遍。
- 沿用既有约定比自创第二套词汇好：mattpocock 的 `.scratch/` 与 5 个 triage 角色是公开标准，沿用保证兼容性；若自创 `my-issues/` 和 `P0/P1/P2`，日后要装 skills 就要改两次。

## M0 设计/开发计划文档落地（12:15–12:25）
- 新增 `docs/design/m0-environment-design.md`（status: draft，待用户评审）：按 feature-design-template 填写——目标/Non-goals（不做 Agent、不上 Astronomy Shop/K8s）、模块表（demo/ + deploy/ + chaos/scenarios/ + datasets/golden/ + src/oncall/scenarios 校验器 + 根 compose）、数据模型（scenario.yaml schema + 黄金集 YAML 双集隔离；六表之 scenarios 表延后到 M7 建）、API（demo /health /tasks /metrics + Alertmanager webhook 落 JSONL）、验收 8 条、T1–T6 任务分解（Phase A=W1 环境跑通+2 剧本，Phase B=剧本补齐+黄金集；TDD 只用在 schema 校验器接缝）。
- 关键边界决策：demo 系统与混沌脚本是"被观测目标"，**不进 `src/oncall`**——coverage 门禁与 import-linter 契约（C3/C4/C5）不适用，物理隔离防架构守卫误伤。
- 4 个开放决策点留给评审：Loki 采集方式（倾向 docker loki driver）、demo/chaos 的 lint 范围（倾向 ruff 覆盖+mypy 豁免）、告警规则与剧本一一对应、`scenario.yaml` 字段名定稿（M7 runner 输入契约，改名牵连大）。
- `m0-execution.md` 头部加回指设计文档的一行。评审通过后下一步：拆 `.scratch/m0-environment/issues/01–08`。

## M0 评审通过与 issue 拆分（12:28–12:35）
- 用户评审通过 → 设计文档 `status: draft → reviewed`，评审人/日期回填（HLGZJY / 2026-09-06），四个开放点按推荐方案落定：①Loki 走 Docker loki logging driver ②ruff 覆盖 demo/chaos、mypy 豁免、coverage 只算 src/oncall ③告警↔剧本映射以 expected_alerts 字段为物理载体 ④scenario.yaml 九字段冻结为 M7 输入契约。
- 新增 decisions.md **D-12**（M0 目录布局 + scenario.yaml 契约冻结）；docs/README.md 快速入口 +1 行。
- 拆 issue 到 `.scratch/m0-environment/`：spec.md（回指设计文档 + 关键契约）+ issues 01–06 对应 T1–T6。状态分配：01/02/03/04/06 = ready-for-agent（验收可机械判定：compose health / curl / Prometheus API / pytest），**05 黄金集 = ready-for-human**（预标注是 M7 判分基准，标注准确性核对权在人，Agent 只辅助采集）；阻塞边 01→02→03→04→05→06 串行。
- 剧本编号方案：01-CPU/02-慢SQL 先行（issue 03），03–11 补齐九个（issue 04），语义层"版本协议不兼容"= 11 号必做。
- 当前 frontier：仅 issue 01（demo 业务系统骨架）可认领。

## 开工 issue 01：demo 骨架落地（12:33–13:00）
- 两个前置提交：`25fd3c1 docs(M0-环境)` 设计定稿 + `233d41d chore(M0-环境)` issue 拆分。
- 认领并实现 issue 01：`demo/common/{settings,db}.py` + `demo/api_gw/main.py`（FastAPI：/health /tasks /metrics）+ `demo/worker/main.py`（Celery，0.2s 模拟延时）+ 双 Dockerfile + 根 `docker-compose.yml`（name: oncall-demo，mysql8/redis7 双 healthcheck，api-gw /health python-urllib 探活）。提交 `bda81db feat(M0-环境)` + `1095a84 test(M0-环境)`。
- **包名坑**：`demo/api-gw/` 目录改 `api_gw/`（Python 标识符不能带连字符，uvicorn 导入会炸），设计文档与 issue 措辞同步回写。
- **ruff 0.16 上浮门禁红（存量）**：①RUF001/002/003 把中文全角标点当歧义字符——连既有 tests 文件都红，全局豁免并注明"仓库语言约定"理由；②PLR2004 魔数 300/200 提常量；③ruff 0.16 会 format docs/*.md 内嵌 Python 代码块——`exclude=["docs"]`。教训：pyproject 的 `ruff>=0.6,<1` 区间太宽，新装环境直接吃到最新版，CI 本地复跑 ruff 才能提前发现。
- **环境**：Docker Desktop 守护进程没开机自启，需先拉起（Start-Process Docker Desktop.exe，WSL2 引擎约 1 分钟就绪）；standalone `docker-compose` v5.5.0 可用但 `docker compose` 插件缺失。mysql:8.0 首拉 ~7 分钟。
- **实测验收全过**：4 容器 healthy；/health 四字段 OK；task1 pending→done（0.25s）；404 路径正确；ruff check + format 全绿。compose 保持运行，供 issue 02 遥测开发直接用。
- 下一步 frontier：issue 02（遥测埋点 + schema 校验器 TDD）。

## 开工 issue 02：遥测 + 校验器（12:52–13:15）
- 提交：`a7269ba feat` 校验器 TDD（src/oncall 首个包——scenarios schema，pydantic extra=forbid 冻结 D-12 契约）+ `4c3893e feat` 遥测接入 Loki/Grafana。issue 02 → resolved。
- **TDD 红绿**：先写 20 用例（红灯 ModuleNotFoundError）→ 实现 schema.py → 绿灯。coverage 97%，30 测试全绿。conftest.py 建 src 导入路径 + 单测默认断网（pytest-socket 可选降级——本地 venv 3.13 装不了 oncall 包因 requires-python<3.12，PYTHONPATH 方案绕开）。
- **坑 1（loki driver）**：docker 日志插件跑在宿主 daemon 网络，loki-url 用 compose 服务名收不到流，必须走宿主发布端口 127.0.0.1:3100；插件 `docker plugin install grafana/loki-docker-driver:latest`（tag 2.3.10 不存在）。
- **坑 2（celery 日志劫持）**：worker 的 structlog JSON 行被包 WARNING 前缀——Celery 配置键必须带 worker_ 前缀（worker_hijack_root_logger/worker_redirect_stdouts），短键名静默无效。
- **坑 3（守卫 dunder）**：__all__ 被 C8 判可变全局，守卫加 dunder 豁免。
- **网络坑**：pip 直连 pypi 失败，用清华源 -i https://pypi.tuna.tsinghua.edu.cn/simple。
- 遥测落地形态：api-gw middleware 埋 QPS/延迟，/metrics 出口前刷 queue_depth/db_pool 两个 Gauge；loki/grafana 端口只绑 127.0.0.1；Grafana 匿名 Admin + provisioning（Loki uid=loki，看板按 compose_service 标签查）。
- 下一步 frontier：issue 03（告警规则 + 首批 2 剧本，W1 验收）。

## Code review 自查与修复（issue 01/02 交付后）
- 五项发现：#1 指标基数（404 回退原始 path）/ #2 worker 失败无终态 / #3 黄金集时间序未校验 / #4 conftest sys.path hack / #5 loki-url 依赖宿主端口（Linux 裸机会失效，换机器改 host-gateway）。#1–#3 已修，#4/#5 记录在案延后。
- #3 走 TDD：3 个新测试先红（resolved/recovered 早于触发时刻）→ AlertEvent/GoldenRun 加 model_validator → 绿灯，coverage 97.65%。
- #2 改为 _mark_failed：置 status=failed + error 日志；连终态都写不进时靠日志暴露（原来 raise RuntimeError 让 celery 任务红但 DB 永远 pending）。
- #1 middleware 未匹配路由 fallback "unmatched"。实测 /nonexistent/1 与 /2 归并为同一标签。
- 提交纪律：fix(M0-环境) 单提交带 review 来源；demo 代码无单测，靠容器实测（health/task 闭环/unmatched 标签）验收。

## 开工 issue 03：告警规则 + 首批 2 剧本（13:55–15:00）
- 交付：deploy/prometheus（prometheus.yml + rules.yml 6 条告警）、deploy/alertmanager（webhook→dump）、deploy/alert-dumper（stdlib 零依赖落 JSONL，M1 换 ingest）、compose 增 3 服务（prometheus v3.4.1 / alertmanager v0.28.1 / alert-dumper，端口只绑 127.0.0.1）、Grafana Prometheus 数据源 provisioning、chaos/scenarios/{01-cpu-spike,02-slow-sql}（inject+cleanup+scenario.yaml 三件套，双过 D-12 校验器）、demo 观测修正 2 处、issue 03 回填置 resolved。
- **Pumba 坑**：gaiaadm/pumba 镜像 entrypoint 就是 pumba，命令里不能再带 `pumba` 前缀（否则 "No help topic for 'pumba'" 秒退）；1.2.0 的 CPU stress 是 `stress --stressors "--cpu 2 --timeout Xs" --inject-cgroup <容器>`，stress-ng 注入目标 cgroup。
- **CPU 飙高可观测性（核心结论，T4 泛化）**：空载 I/O 型端点在 CFS 公平调度下饿不出延迟（单核 100% 时 P95 仅 10ms）；必须 ①runtime 收窄 cpuset（docker update，配基线记录文件防重复注入把 '0' 当基线）②叠加业务探针流量（宿主机循环 curl 在 Windows/git bash 大规模派生进程不可靠→改为单容器 128 线程 python 探针）；且 demo_request_duration_seconds 是服务端计时，TCP 侧排队不计入→阈值定 0.05s（健康负载 5ms / 饥饿负载 95ms），不能靠堆流量顶过任意阈值。
- **正交性修正**：/health 原本共享主连接池，慢 SQL 打满池会连带 CPU 剧本告警误报→demo/common/db.py 增 health_engine（独立小池 pool_size=2, timeout=30 排队不报错），/health 改走 check_db_direct——"健康检查不得与被观测故障共沉浮"。
- **5xx 失明坑**：端点未处理异常绕过 metrics 中间件（call_next raise 后 inc() 不执行），demo_requests_total 对崩溃型 500 完全无数据；except 分支补记修复。连带发现 scope["route"] 在路由匹配后才写入，提前读取全部落 unmatched——endpoint 提取必须放在 call_next 返回/抛出之后（封装 _endpoint()）。
- **+Inf 桶 NaN 陷阱**：全部延迟样本落 +Inf 时 histogram_quantile 返回 NaN，NaN 比较恒假→DemoTasksHighLatency 加 `or 5xx rate>0.02` 兜底支路。
- **慢 SQL 剧本要点**：LOCK TABLES tasks WRITE + SELECT SLEEP(300)（docker exec -d）；探针必须 12 线程并发 POST /tasks——串行单请求只占 1 连接，池饱和与告警样本都出不来；cleanup 用 processlist `command='Query' AND info LIKE '%SLEEP(%'` 定位持锁会话 KILL。
- 实测数据：cpu-spike 非任务路径 P95 0.0095→0.091s，DemoApiGwHighLatency 3 轮 firing/resolved；slow-sql db_pool_used=10，DemoDbPoolSaturated/DemoTasksHighLatency/DemoHighErrorRate 全 firing，cleanup 后 90–120s 全恢复；alerts-dump.jsonl 共 25 条记录。每剧本 ≥3 轮重复执行验证。
- 下一步 frontier：issue 04（T4 注入框架泛化 + 剩余 9 剧本），从 01/02 提炼三件套目录约定。

## 开工 issue 04：注入框架泛化 + 剧本 03–11（15:35–17:40）
- 提交：`dc41863 feat` demo 观测面扩展与正交性修复 + `e9e1f5a feat` 剧本 03–11 至 11 个/规则 8 条 + `b8244d6 docs` issue 04 回填置 resolved。issue 04 → resolved。
- TDD 红绿：新增 `tests/unit/test_scenario_catalog.py`（剧本目录守卫：全量 schema 校验 / ≥8 剧本 / 六类全覆盖 / 语义层必存在 / expected_alerts ⊆ rules.yml 已配置规则名）——先红（2 剧本缺 4 类）后绿，38 测试全绿。
- demo 观测面扩展：GET /tasks/{id} 缓存（TTL 60s）+ hit/miss Counter、create_task 带协议版本、worker redis 门禁 chaos:min_protocol（11 号注入点）、STALE_PENDING gauge；`docker-compose build/up --no-deps` 滚动更新（本机无 compose 插件，用 standalone docker-compose）。
- **9 剧本全部容器实测闭环**（注入→firing→cleanup→恢复，最终 ALERTS 为空）：03 OOM ~25s 确定性 OOMKilled；04/05 网络 700ms 延迟/25% 丢包 up 保持 1；06 死锁 12 INSERT 等待 supremum 锁；07 积压 1374 条排空；08 池 1m 均值 >70%；09 SIGKILL worker 扩容加速恢复；10 miss ~160/s；11 语义层无 5xx 无资源异常仅业务指标告警。
- **实测揪出三个缺陷（均修复）**：①demo_queue_depth 读 redis db0 而 broker 在 db1 恒为 0——DemoQueueDepthHigh 是死规则，新增 broker_client；②stale pending 统计让 /metrics 首次依赖主连接池，死锁剧本打满池 → 抓取失联 → DemoApiGwDown 误触发——新增 metrics_engine（read_timeout=2s）专用引擎，指标端点与故障脱钩；③DemoDbPoolSaturated 刚性 used>=size 在纯负载下 flaky（瞬时抖动 min 到 2）——改 1m 平均占用 ≥70% 池容量。
- **Pumba 新坑（Docker Desktop/WSL2）**：①`--ingress-port/--egress-port` 端口过滤不生效（api-gw 出向/mysql 入向两种目标实测均被忽略）——网络类只能全流量损伤，取值以"顶过告警阈值且 < scrape_timeout 4s"为界（3s 延迟会打瞎观测面）；②`--duration` 必须 < `--interval`（周期重施语义）；③--interval 值必须带时间单位（"1s" 非 "1"）。
- 负载探针经验：短连接 urllib 客户端开销盖过服务端（池占用只到 3~4），必须 keep-alive + 多进程（8~16×24 线程）才能稳定打满池；单进程多线程受 GIL 锯齿化。
- 10 号缓存雪崩的 FLUSH 间隔教训：10s 级不行——热点键秒级回填 miss 窗口一闪而过（2.6/s），50ms 间隔才等效缓存持续不可用（~160/s）。
- 下一步 frontier：issue 05（黄金集生成，ready-for-human，标注核对权在人）。

## M1 开发计划文档预生成（18:05–18:30，M0-04 已 resolved 后的收尾间隙）
- 用户意图：趁 M0 收尾（05/06 未决）为「第 2 个任务 = M1 告警接入与归一化」预生成开发计划文档；已拍板两份都建 + 本次不建 `.scratch/m1-*`（等评审 grill 后正式进入 agent 阶段再拆 issue）。
- 新增 `docs/plans/m1-execution.md`（status: seed，仿 m0-execution）：技术类别与边界四组、接入步骤、指纹去重约定（默认值+待 grill）、上下文三源、T1–T6 拆解、验收口径。
- 新增 `docs/design/m1-alert-ingestion-design.md`（status: draft，仿 m0-environment-design + feature-design-template）：含「是否已正式进入 agent 阶段状态说明」（结论=未进入，5 条进入条件表）、目标/Non-goals（零 LLM、不建 incidents、不算降噪率）、本阶段技术类别、技术方案（模块/数据模型/API 三表）、验收、依赖、开放设计点 G1–G7（指纹字段与 10min 窗、去重落库、事件卡片 JSON 载体、变更源占位、alert_events 增列、receiver 双写回退、与 M2 边界）、T1–T6 任务拆解（里程碑 M1-A/B/C；Phase A 并行纪律=不切 receiver，切换在 T6 且须 M0-05 后）。
- 更新 `docs/README.md` 快速入口 +1 行（M1 设计+细案，注明 draft/未进入 agent/issue 待建）。
- 校准：开场用户说「04 仍在开发」，git 实况 04 已 resolved（b8244d6）——文档按仓库真值表述（前置依赖 03/04 双 ✅，剩 05/06）。未提交（用户无要求）。

## M1 评审通过与 issue 拆分（18:30–18:55）
- 用户指令：开始评审 G1–G7，评审标准检索 GitHub 引用来源，并创建 `.scratch/m1-alert-ingestion/`。
- **检索到的标准来源（已固化进设计文档 §7「评审依据」R1–R5）**：R1 Alertmanager webhook v4 契约（prometheus.io/docs/alerting/latest/configuration#webhook_config + github.com/prometheus/alertmanager）——`alerts[]` 批量数组、`truncatedAlerts`、每条 alert 自带 `fingerprint`、2xx 确认/非 2xx 指数退避重试；R2 prometheus/common 指纹算法（model/signature.go、fingerprinting.go）——FNV-1a 64bit、label 排序 + SeparatorByte(0xFF)、`SignatureWithoutLabels` 排除易变 label 先例；R3 nflog/DedupStage 去重状态持久化（notify/dedup_stage.go + high_availability 文档）；R4 group_wait/group_interval/repeat_interval 时间窗语义（AM README）；R5 sqlite.org/whentouse（低流量服务端/企业 RDBMS 替身）。
- **评审带来的三个设计修订（超出原推荐解的部分）**：①主指纹自算（canonical 子集 sha256），AM 自带 fingerprint 含易变 label 只存 annotations_json 溯源；②`/ingest` 幂等为硬要求（重试投递语义）→ 验收加"同 payload 重放 3 次 dedup_count 不变"；③归一化按批量数组处理，不假设单条。
- G1–G7 全部按推荐解定案（G1 canonical={alertname,job,instance}+0xFF 分隔 sha256、10m 可配窗；G2 落 DB 行不引 Redis；G3 GET /alerts/{id}/context JSON；G4 变更源占位；G5 增列四处；G6 双写开关；G7 边界不变）。
- **落盘**：设计文档 `draft → reviewed`（评审人栏注明"用户授权 AI 执行，可推翻回退"）；CONTEXT.md 新增「归一化/Normalization」「事件卡片/Alert Card」两术语；架构文档 §4 alert_events 行追加增列标注；decisions.md 新增 **D-13**（增列 + 指纹/去重/幂等契约）；m1-execution.md 同步（状态说明、G1 定案、T3–T5 就绪态）。
- **新建 `.scratch/m1-alert-ingestion/`**：spec.md（关键契约冻结区）+ issues/01–06 对应 T1–T6，**六票全部 ready-for-agent**（开放点已定案故 T3–T5 不再 ready-for-human）；阻塞边 01→{02→03,02→04}→05→06；06 额外 Blocked by M0-05（receiver 切换硬约束）。
- 同文件并行 Edit 坑再现：设计文档评审时残留一处旧状态说明（§status 副节结论句），靠 grep 复核发现后串行修复——m1-execution.md 全部串行编辑，一次通过。
- 未提交（git status 另有 src/oncall/scenarios、tests 等他人/其他会话改动，不碰）。frontier：issue 01（alert_events 表）可立即派工，issue 05 黄金集（M0）仍待人核对。
- M1 改动落为两个提交：`7eb6597 docs(M1-告警接入)` 评审定案+回写（6 文件/242 行）、`e58f545 chore(M1-告警接入)` issue 跟踪器（8 文件/235 行）。工作区剩余 `src/oncall/scenarios`、`tests/unit/test_golden_tree.py`、`datasets/` 为 M0-05 在途工作，未提交。用户随即自行开工 issue 01。

## M1-T1 落地：alert_events 表与模型（19:10–19:40）
- 提交 `36928ba feat(M1-告警接入)`（4 文件/242 行）：`src/oncall/db/{__init__,models}.py` + `tests/unit/test_db_alert_events.py` + issue 01 回填置 resolved。issue 01 → resolved。
- 模型要点：10 列 = 架构 §4 冻结六列 + D-13 增列四列；fingerprint String(64) unique+index（sha256 hex，G1 指纹计算在 T3 应用层）；status 默认 deduped + CHECK 冻结取值；dedup_count 默认 1；last_fired_at 首次由 fired_at 回填（validates）；annotations_json 默认 {}。dev 建表 create_all（Alembic 延至 MySQL）。
- TDD 红绿：10 用例先红（ModuleNotFoundError）后绿。两个红灯均为测试自身 bug：①SQLite 方言 get_indexes 的 unique 返回 1（int）非 True——truthy 断言即可；②重复测试笔误插了不同 fingerprint。模型无改动。
- **同文件并行 Edit 丢改动坑第三次复现**：测试文件两个 Edit 并行发，第一个静默丢失（报成功未写入），靠测试失败暴露后串行重发。铁律再次验证：同文件多次编辑必须串行。
- 环境：sqlalchemy 2.0.52 装入 `C:/Users/heguo/.workbuddy/binaries/python/envs/default` venv（此前只有 pytest/pydantic/coverage，缺 sqlalchemy；清华源直装成功）。测试跑法：`envs/default/Scripts/python.exe -m pytest tests -q --cov=src/oncall`（PYTHONPATH 由 conftest 注入）。
- 隔离提交验证：暂存区精确 4 文件，M0-05 在途改动（scenarios/golden_tree/datasets/工作日志）全部留在工作区未提交。
- 下一步 frontier：issue 02（/ingest webhook + 归一化，依赖 T1 ✅）。

## 开工 issue 05：黄金集首批采集（18:20–19:55，4 剧本 ×3 轮）
- 提交：`818d75b feat` 双集目录树校验器 TDD + `c939506 feat` 首批采集草稿与工具。issue 05 保持 ready-for-human。
- TDD 接缝：schema 层 GoldenSet.runs 放宽 ≥1；新增 load_golden_tree 强制 dev≥2+holdout≥1（=×3）、slug 双集成对、文件名↔scenario 一致、同一 run 禁跨集复制。红灯 10 用例 → 绿灯，coverage 97.86%，58 测试全绿。
- 采集：11/04/05/06 各 ×3 轮（12 次闭环，单轮 4.5–8.5min），工具 datasets/golden/tools/{collect_run.sh,build_yaml.py}。草稿 dev=r1+r2 / holdout=r3，全部过树校验；fired_at/resolved_at 取 alerts-dump.jsonl 实测值按指纹配对，切片留档 _raw/。
- **坑 1（scenario 标签陷阱）**：rules.yml 的 scenario label 是静态来源标注（DemoApiGwHighLatency 固定标 cpu-spike）——按 scenario=slug 过滤 ALERTS 判定触发会漏判（04 号首轮因此 FAIL）。判定只能用 alertname ∈ expected_alerts。
- **坑 2（dump 并发截断）**：baseline 按行数切片时遇 alert-dumper 正在追加，可能切出半行 JSON——解析器跳过坏行即可，告警配对不受影响。
- 遗留：剩余 7 剧本采集顺延（同一套工具复用）；人工抽核 ≥1 剧本标注后双签回填（Comments 入口已建）；两个 idle 观察容器（vibrant_chatterjee/objective_germain，tail -f /dev/null）可手动清理。
